<Cluster xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://wso2.com/schema/clustering/config cluster.xsd"
         xmlns="http://wso2.com/schema/clustering/config">

    <!--Indicates whether clustering should be enabled or disabled for this node-->
    <Enable>false</Enable>

    <!-- The agent implementation used with cluster framework-->
    <Agent>hazelcast</Agent>
    <!--
     The clustering domain/group. Nodes in the same group will belong to the same multicast
     domain. There will not be interference between nodes in different groups.
    -->
    <Domain>wso2.carbon.domain</Domain>

    <!-- Properties specific to this member -->
    <LocalMember>
        <Host>127.0.0.1</Host>
        <Port>4000</Port>

        <Properties>
            <Property name="backendServerUrl">https://${hostName}:${httpsPort}/services/</Property>
            <Property name="mgtConsoleUrl">https://${hostName}:${httpsPort}/</Property>
            <Property name="subDomain">worker</Property>
        </Properties>
    </LocalMember>

    <!--
        The membership discovery scheme used in this setup. Multicast, WKA, AWS & Generic are the supported
        schemes.

        1. Multicast - membership is automatically discovered using multicasting
        2. WKA - Well-Known Address based discovery. Membership is discovered with the help
            of one or more nodes running at a Well-Known Address.
        3. AWS - membership scheme for Amazon AWS deployments
        4. Generic - allows plugging in custom membership schemes
    -->
    <MembershipScheme>
        <!-- multicast membership scheme related properties-->
        <Multicast>
            <Group>228.0.0.4</Group>
            <Port>45564</Port>
            <TTL>100</TTL>
            <!--<Timeout>60</Timeout>-->
        </Multicast>

        <!-- wka membership scheme related properties-->
        <!--WKA>
            <Members>
                <Member>
                    <Host>127.0.0.1</Host>
                    <Port>4000</Port>
                </Member>
            </Members>
        </WKA-->

        <!--AWS membership scheme related properties-->
        <!--AWS>
            <AccessKey>xxxxxxxxxx</AccessKey>
            <SecretKey>yyyyyyyyyy</SecretKey>
            <SecurityGroup>a_group_name</SecurityGroup>
            <Region>us-east-1</Region>
            <TagKey>a_tag_key</TagKey>
            <TagValue>a_tag_value</TagValue>
        </AWS-->

        <!--Generic membership scheme related properties-->
        <!--Generic>
            <Name>Foo</Name>
            <Parameters>
                <Parameter name="xxx">xxx-val</Parameter>
                <Parameter name="yyy">yyy-val</Parameter>
                <Parameter name="zzz">zzz-val</Parameter>
            </Parameters>
        </Generic-->
    </MembershipScheme>

</Cluster>